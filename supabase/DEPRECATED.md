# Deprecated

This directory is no longer the production Supabase project.

Use the monorepo root `supabase/` for migrations and Edge Functions (`ingest`, `pair-*`, workbench APIs).
Do not edit or deploy `no_swipe/supabase/functions/ingest`.
