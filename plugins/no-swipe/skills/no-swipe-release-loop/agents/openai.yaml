interface:
  display_name: "No Swipe 候选闭环验收"
  short_description: "安装候选、双浏览器验收、问题归因、迭代修复与发布把关"
  default_prompt: "Use $no-swipe-release-loop to build and install a No Swipe candidate, verify the ordinary browser priority is connected external Chrome, then connected external Edge, then the Codex built-in browser only before any page action, verify Safari is never selected, and keep the selected browser fixed after the first page action. In every new Codex task open https://www.douyin.com/user/self as the first Douyin identity action while the workbench remains in a separate tab, retry once before a unique exact nickname match from the visible current Douyin page, account menu, or account avatar, never open another creator's homepage, run closed-loop acceptance in the required browser modes, diagnose failures, iterate, and report the release gate."
