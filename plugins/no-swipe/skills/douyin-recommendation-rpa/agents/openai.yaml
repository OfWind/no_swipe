interface:
  display_name: "抖音推荐流一键配置"
  short_description: "授权上传后确认账号方案，创建目标并连续执行推荐流"
  default_prompt: "Use $douyin-recommendation-rpa to open https://www.douyin.com/user/self as the first Douyin identity action in every new Codex task while No Swipe authorization starts in a separate workbench tab, retry the self page once before a unique exact nickname match from the visible current Douyin page, account menu, or account avatar, never open another creator's homepage, confirm one compact preset, create a durable Goal, and execute the run with concise progress updates."
