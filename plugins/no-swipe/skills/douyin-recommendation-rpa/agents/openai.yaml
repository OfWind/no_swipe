interface:
  display_name: "抖音账号画像与推荐流 RPA"
  short_description: "复用账号画像，确认每轮比例授权后采集推荐流"
  default_prompt: "Use $douyin-recommendation-rpa to reuse or bind the active account profile, prepare an explicit RunConfig, request confirmation, and only then run or audit the recommendation feed."
